# 작업 노트 (AI Work Log)

## goal
- Elasticsearch가 내려가 있는 환경에서도 `/actuator/health` 호출 시 `Connection refused` 스택트레이스가 발생하지 않도록 한다.

## key changes
- 기본 설정(`application.yml`)에서 Actuator Elasticsearch HealthIndicator를 비활성화: `management.health.elasticsearch.enabled=false`
- `dev-search` 프로파일에서만 HealthIndicator를 다시 활성화: `application-dev-search.yml`에 `management.health.elasticsearch.enabled=true`

## touched files
- `src/main/resources/application.yml`
- `src/main/resources/application-dev-search.yml`

## run/test commands
- `./mvnw -DskipTests test -q`

## follow-ups
- dev-search 기능을 완전 옵션으로 두려면 ES auto-config / repositories 자체를 프로파일 기반으로 분리하는 구성이 더 깔끔하다.
