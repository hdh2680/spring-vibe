# 작업 노트 (AI Work Log)

## goal
- 애플리케이션 구동 시 `velogPostRepository`가 중복 등록되어 실패하는 문제를 해결한다.

## key changes
- 메인 웹 앱 구동 시 `VelogIngestApplication`(one-shot 도구용 `@SpringBootApplication`)이 컴포넌트 스캔에 의해 함께 로딩되며 JPA repository 스캔이 중복 실행되는 것을 방지했다.
- `VelogIngestApplication`에 `@Profile("velog-ingest")`를 추가하고, 해당 main 실행 시 `.profiles("velog-ingest")`로 프로파일을 자동 활성화하도록 변경했다.

## touched files
- `src/main/java/springVibe/etc/velog/VelogIngestApplication.java`

## run/test commands
- `./mvnw -DskipTests compile`

## follow-ups
- one-shot 도구 앱을 별도 모듈로 분리하거나(추천), 메인 앱의 `@ComponentScan`에서 특정 설정 클래스를 exclude 하는 방식도 대안이다.
