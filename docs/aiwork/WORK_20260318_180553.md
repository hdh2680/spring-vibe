# 작업 노트 (2026-03-18 18:05:53 KST)

## goal
- PRD 전처리 권장안 반영: 기본 정규화 + 노이즈 제거 + 반복 축약 + 멘션/해시태그 처리 + (옵션) 스팸/중복/언어 분리

## key changes
- 전처리(`textClean`)에 아래 처리를 추가
  - 유니코드 정규화(NFKC), 개행/탭 -> 공백, 공백 축약
  - HTML 엔티티(기본) 및 태그 제거
  - 멘션/해시태그 토큰화: `@...` -> `@MENTION`, `#...` -> `#TAG`
  - 반복 축약: `ㅋㅋㅋㅋ`/`ㅎㅎㅎㅎ`/`ㅠㅠㅠ`/`!!!!`/`???`/`...` 등 의미 보존 형태로 축약
  - 이모지는 `EMOJI` 토큰으로 치환 후 정리
- 옵션 성격의 신호는 “삭제” 대신 “표시”로 추가
  - `languageHint` (ko/other/unknown)
  - `isDuplicate` (동일 `textClean` 중복 여부)
  - `isSpamCandidate`, `spamReason` (가벼운 룰 기반)

## touched files
- `src/main/java/springVibe/dev/users/youtubeComment/service/YoutubeCommentService.java`
- `docs/aiwork/WORK_20260318_180553.md`

## run/test commands
- `./mvnw -q -DskipTests compile` (Windows: `mvnw.cmd`)

## follow-ups
- 스팸 룰/토큰화 정책(멘션/해시태그 제거 vs 토큰화) 확정 시 설정화 고려

