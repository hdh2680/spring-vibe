# 목표 (goal)
`youtubeCommentHistoryView.html`에서 댓글 **원본 텍스트**와 **전처리 텍스트**를 모두 보여주고, 목록 영역이 길어져도 페이지가 과도하게 늘어나지 않도록 **overflow scroll** 처리한다.

# 핵심 변경사항 (key changes)
- 전처리 미리보기 로드 시 `textOriginal` / `textClean`을 함께 읽어 화면에 노출하도록 DTO를 확장했다.
- 상세 화면 테이블을 `Original Text` / `Preprocessed Text` 2개 컬럼으로 분리했고, 테이블 컨테이너에 `max-height` + `overflow: auto`를 적용해 스크롤되도록 했다.

# 수정/추가한 파일 (touched files)
- `src/main/java/springVibe/dev/users/youtubeComment/service/YoutubeCommentService.java`
- `src/main/resources/templates/html/users/youtubeComment/youtubeCommentHistoryView.html`

# 실행/테스트한 명령 (run/test commands)
- (없음)

# 후속 할 일 (follow-ups)
- 필요 시 테이블 헤더 고정(sticky) 적용 여부 검토.
- 모바일 화면에서 컬럼 폭/가독성 개선 필요 여부 확인.

