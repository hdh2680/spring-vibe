# 목표 (goal)
- 감정 분석 탭의 `감정 분포` 표현을 막대(bar) 형태에서 파이(pie) 차트로 변경한다.

# 핵심 변경사항 (key changes)
- `sentOverall` 렌더링 로직을 SVG 파이 차트 + 레전드(라벨/색상/비율) 형태로 변경했다.
- 기존 막대형 UI(`sent-row`) 렌더링은 제거하고, 반응형 레이아웃을 위해 `sent-overall`, `sent-pie-legend` 스타일을 추가했다.

# 수정/추가 파일 (touched files)
- `src/main/resources/templates/html/users/youtubeComment/youtubeCommentAnalysisView.html`
- `docs/aiwork/WORK_20260320_160208.md`

# 실행/테스트 명령 (run/test commands)
- (미실행) `mvn test`

# 후속 작업 (follow-ups)
- 파이 차트에 마우스오버 툴팁(라벨/비율) 또는 선택 강조 효과를 추가할지 UI 정책 확정.
