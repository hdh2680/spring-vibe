# AI Work Log

- goal: 로컬 개발(5173)에서도 React 헤더의 로고/메인/로그인 링크가 Spring(8080)으로 이동하도록 분기 처리
- key changes:
  - Vite 환경 플래그(`import.meta.env.DEV`)로 dev일 때만 Spring origin(`http://localhost:8080`)을 링크에 prefix
  - 운영(8080에서 `/app` 서빙)에서는 상대 경로(`/main`, `/login` 등)로 동일 오리진 이동
- touched files:
  - `frontend/src/shared/components/SvHeader.tsx`
- run/test commands:
  - `Get-Date -Format yyyyMMdd_HHmmss`

