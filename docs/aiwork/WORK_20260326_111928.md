# 작업 노트 (AI Work Log)

## goal
- `PRD-devSearch`에 로컬에서 Elasticsearch를 실행/확인하는 방법을 추가한다.

## key changes
- `docker-compose.dev-search.yml` 기반 실행/상태확인/동작확인/종료 명령을 문서에 추가했다.
- `local` 프로파일에서 Dev Search를 켜기 위한 환경변수(`DEV_SEARCH_ENABLED`, `ELASTICSEARCH_URIS`) 예시를 추가했다.

## touched files
- `docs/PRD-devSearch.md`

## run/test commands
- `./mvnw -DskipTests compile`

## follow-ups
- 필요 시 Windows/WSL 환경에 따른 `curl`/`Invoke-WebRequest` 대체 예시를 추가한다.
