# 작업 노트 (AI Work Log)

## goal
- `DEVSEARCH_DISABLED (repositories not enabled)`가 발생하는 설정 불일치(기능 플래그는 on인데 repository는 off)를 방지한다.

## key changes
- `application-local.yml`에서 Elasticsearch repository/health enablement를 `dev-search.elasticsearch.enabled` 값에 직접 연결했다.
  - `spring.data.elasticsearch.repositories.enabled: ${dev-search.elasticsearch.enabled:false}`
  - `management.health.elasticsearch.enabled: ${dev-search.elasticsearch.enabled:false}`
- `dev-search.elasticsearch.enabled` 기본값을 `false`로 수정했다(기존 `true`는 오타로 보이는 상태).

## touched files
- `src/main/resources/application-local.yml`

## run/test commands
- `./mvnw -DskipTests compile`

## follow-ups
- 실행 시 `DEV_SEARCH_ENABLED=true`만 주면 기능/레포/헬스체크가 한 번에 켜지도록 유지한다.
