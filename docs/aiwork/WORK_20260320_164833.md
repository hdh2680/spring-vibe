# 목표 (goal)
- 특정 욕설/비난 댓글이 긍정으로 오분류되는 문제를 커스텀 사전 보강으로 완화한다.

# 핵심 변경사항 (key changes)
- `sentiment_custom.tsv`에 다음을 반영했다.
  - 일반 부사 `제대로`가 base 사전에서 긍정(+1)으로 잡혀 오분류를 유발하므로 0으로 override
  - 강한 부정 문맥을 담는 표현을 부정(-1)으로 추가: `나라망신`, `죽어라`, `우물안의 개구리`

# 수정/추가 파일 (touched files)
- `src/main/resources/static/docs/sentiment_custom.tsv`
- `docs/aiwork/WORK_20260320_164833.md`

# 실행/테스트 명령 (run/test commands)
- (미실행) `mvnw -DskipTests compile`

# 후속 작업 (follow-ups)
- 오분류 샘플을 더 모아 `sentiment_custom.tsv`에 noisy term(예: 강조 부사) 중립화와 도메인 부정 표현 확장을 반복한다.
