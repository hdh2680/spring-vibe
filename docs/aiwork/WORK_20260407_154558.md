# AI Work Log

- goal: React `/app` 헤더에서 불필요한 `Main(Thymeleaf)` 링크 제거
- key changes:
  - `SvHeader`에서 `Main(Thymeleaf)` 버튼 및 구분선 제거, `Login`만 유지
- touched files:
  - `frontend/src/shared/components/SvHeader.tsx`
- run/test commands:
  - `Get-Date -Format yyyyMMdd_HHmmss`

