# 목표 (goal)
- 감정분석 탭의 시간별 변화 그래프에서, 미분류를 제외한 비율로 인해 100%로 요동치는 현상을 완화하기 위해 **미분류를 포함한(total 기준) 비율 데이터**를 사용한다.
- 단, 시각화에서는 미분류 라인을 표시하지 않는다.

# 핵심 변경사항 (key changes)
- 시간별 변화 SVG에 `unknownPct` polyline을 다시 추가하되, `.unknown-line { display:none; }`로 숨겼다.
  - 결과적으로 `positivePct/neutralPct/negativePct`는 `total(미분류 포함)` 기준 비율을 그대로 사용한다.

# 수정/추가 파일 (touched files)
- `src/main/resources/templates/html/users/youtubeComment/youtubeCommentAnalysisView.html`
- `docs/aiwork/WORK_20260320_165844.md`

# 실행/테스트 명령 (run/test commands)
- (미실행) `mvnw -DskipTests compile`

# 후속 작업 (follow-ups)
- 필요 시 UI 토글로 `미분류 라인 표시/숨김` 기능 제공.
