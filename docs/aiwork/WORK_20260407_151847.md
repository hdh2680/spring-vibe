# AI Work Log

- goal: React(`/app`)에서 Spring(Thymeleaf) UI와 유사한 헤더/레이아웃을 갖추기 위한 기본 Header 컴포넌트 추가
- key changes:
  - React `index.html`에서 Spring CSS(`/css/brand.css`, `/css/layout_app.css`)를 로드하도록 추가
  - React 레이아웃을 `.layout`/`.sv-header`/`.sv-main`/`.content` 구조로 전환
  - React 헤더 컴포넌트 추가(Thymeleaf `layout/app.html`과 동일한 클래스명 기반): `SvHeader`
  - 홈 화면을 Spring 공통 컴포넌트 스타일(`panel`, `toolbar`, `muted`)에 맞춰 정리
- touched files:
  - `frontend/index.html`
  - `frontend/src/main.tsx`
  - `frontend/src/styles/app.css`
  - `frontend/src/shared/components/SvHeader.tsx`
  - `frontend/src/app/ui/AppLayout.tsx`
  - `frontend/src/features/home/HomePage.tsx`
- run/test commands:
  - `Get-Content ...`
  - `rg ...`
  - `Get-Date -Format yyyyMMdd_HHmmss`
- follow-ups:
  - 실제 메뉴(좌측/상단) 구성 확정 시: Thymeleaf 메뉴 모델(`leftMenus`)과 `/app` 메뉴를 어떻게 통합/분리할지 결정

