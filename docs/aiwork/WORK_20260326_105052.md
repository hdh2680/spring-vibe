# 작업 노트 (AI Work Log)

## goal
- `local` 프로파일 + `DEV_SEARCH_ENABLED` 토글만으로 Dev Search(Elasticsearch)를 운용하기 위해 `dev-search` 프로파일 의존을 제거한다.

## key changes
- `application-dev-search.yml`을 삭제했다(더 이상 사용하지 않음).
- DevSearch 비활성화 안내 메시지를 `dev-search` 프로파일 활성화가 아닌 `DEV_SEARCH_ENABLED=true` 토글 기준으로 변경했다.

## touched files
- `src/main/resources/application-dev-search.yml` (deleted)
- `src/main/java/springVibe/dev/users/devSearch/search/DevSearchService.java`

## run/test commands
- `./mvnw -DskipTests compile`

## follow-ups
- 추후 환경별로 Dev Search를 켜야 하면(예: staging) 별도 프로파일을 다시 추가하거나, 환경변수/시크릿 매니저 기반 설정으로 확장한다.
