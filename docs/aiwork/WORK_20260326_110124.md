# 작업 노트 (AI Work Log)

## goal
- `PRD-devSearch` 문서의 `[검색서비스]` 단락을 현재 구현/설정 기준으로 구체화한다.

## key changes
- `local` 프로파일 + `DEV_SEARCH_ENABLED` 토글 기반으로 Dev Search를 제어하는 방식을 명시했다.
- 현재 구현된 REST API(`/api/dev-search/*`), 인덱스 생성 전략(createIndex=false + lazy ensureIndex), 장애 시 동작(ES down)을 문서화했다.
- 목록/상세(Thymeleaf) 및 “ES로 pk 추출 후 DB 조회” 방식은 목표/예정 범위로 분리해 명확히 했다.

## touched files
- `docs/PRD-devSearch.md`

## run/test commands
- `./mvnw -DskipTests compile`

## follow-ups
- 실제 목록/상세 화면 및 DB 기반 조회 구현이 완료되면, “예정” 항목을 “구현”으로 승격하고 엔드포인트/템플릿 경로를 확정한다.
