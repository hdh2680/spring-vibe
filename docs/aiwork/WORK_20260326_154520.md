# 작업 노트 (AI Work Log)

## goal
- Windows PowerShell에서 Dev Search를 켠 상태로 `local` 프로파일로 실행하는 검증된 커맨드를 문서에 남긴다.

## key changes
- `PRD-devSearch`에 아래 실행 예시를 추가했다.
  - `$env:DEV_SEARCH_ENABLED="true"`
  - `.\mvnw.cmd spring-boot:run "-Dspring.profiles.active=local"`

## touched files
- `docs/PRD-devSearch.md`

## run/test commands
- `./mvnw -DskipTests compile`

## follow-ups
- 필요 시 `ELASTICSEARCH_URIS`를 함께 지정하는 예시를 유지/확장한다.
