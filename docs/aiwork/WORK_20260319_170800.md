# 목표 (goal)
분석 결과 화면에서 주제 분포를 스택 바 대신 도넛 차트로 시각화한다.

# 핵심 변경사항 (key changes)
- `youtubeCommentAnalysisView.html`의 "주제 분포" 영역을 도넛 차트(Conic gradient)로 변경했다.
- 렌더링 시 퍼센트 누적(gradStart/end)을 사용해 도넛의 각 세그먼트를 정확하게 생성하고, 마지막 세그먼트는 100%로 맞춰 시각적 갭을 방지했다.

# 수정/추가한 파일 (touched files)
- `src/main/resources/templates/html/users/youtubeComment/youtubeCommentAnalysisView.html`

# 실행/테스트한 명령 (run/test commands)
- `.\mvnw.cmd -q -DskipTests compile`

# 후속 할 일 (follow-ups)
- 도넛 세그먼트에 hover 툴팁(주제명/비율) UX 개선 여부 검토.