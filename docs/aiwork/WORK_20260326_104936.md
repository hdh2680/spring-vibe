# 작업 노트 (AI Work Log)

## goal
- `local` 프로파일만 사용하면서도 Elasticsearch(Dev Search)를 환경변수로 켜고 끌 수 있게 한다.

## key changes
- `application-local.yml`에 `DEV_SEARCH_ENABLED` 환경변수 기반 토글을 추가했다.
  - `dev-search.elasticsearch.enabled`
  - `spring.data.elasticsearch.repositories.enabled`
  - `management.health.elasticsearch.enabled`
- Elasticsearch URI는 `ELASTICSEARCH_URIS`로 오버라이드 가능하도록 했다(기본: `http://localhost:9200`).

## touched files
- `src/main/resources/application-local.yml`

## run/test commands
- `./mvnw -DskipTests compile`

## follow-ups
- `application-local.yml`은 gitignore 대상이어야 하며, 외부 API 키 등 시크릿은 반드시 로컬/환경변수로만 관리한다.
