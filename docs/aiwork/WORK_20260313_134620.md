# AI Work Log (20260313_134620)

- 목표(goal): 프로젝트 로그 레벨을 DEBUG로 상향
- 핵심 변경사항(key changes): application.yml에 logging.level(root, springVibe) = DEBUG 설정 추가
- 수정/추가한 파일(touched files):
  - /src/main/resources/application.yml
  - /docs/aiwork/WORK_20260313_134620.md
- 실행/테스트한 명령(run/test commands):
  - .\\mvnw.cmd -q -DskipTests compile
- 후속 할 일(follow-ups):
  - 운영 환경에서는 INFO/WARN으로 낮추고, 패키지별로 DEBUG를 제한하는 프로파일 분리 권장
