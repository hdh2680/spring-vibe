# WORK_20260312_174135

## Goal
- Record login history to `login_logs` on every login attempt (SUCCESS/FAIL).
- Re-check alignment with `docs/ARCHITECTURE.md` and `docs/CODING_CONVENTION.md`.
- Update docs (`FUNCTIONS.md`, `TODOLIST.md`, `USER_GUIDE.md`).

## Key Changes
- Added MyBatis mapper + XML for inserting into `login_logs`.
- Added Spring Security auth event listeners to log SUCCESS/FAIL with IP and User-Agent.
- Refactored auth data access behind a `dev.service` to match the "mapper/repository injected into service" pattern.

## Notes On Rules
- Package naming, class naming, indentation, and max line length (<= 120) are respected in the touched Java sources.
- The previous direct mapper usage from `system.security` was moved behind `dev.service.AuthService` to better follow the architecture guideline.

## Files Touched
- `src/main/java/basemarkdown/dev/domain/LoginLog.java`
- `src/main/java/basemarkdown/dev/mapper/LoginLogMapper.java`
- `src/main/resources/mapper/LoginLogMapper.xml`
- `src/main/java/basemarkdown/system/security/LoginAuditListener.java`
- `src/main/java/basemarkdown/dev/service/AuthService.java`
- `src/main/java/basemarkdown/system/security/DatabaseUserDetailsService.java`
- `docs/FUNCTIONS.md`
- `docs/USER_GUIDE.md`
- `docs/TODOLIST.md`

## Commands
- `./mvnw.cmd clean test`

## Follow-ups
- Consider enabling CSRF for form login and add the CSRF token field to `login.html`.
