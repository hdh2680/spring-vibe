# 작업 노트 (AI Work Log)

## goal
- `src/main/resources/static/docs` 하위의 유튜브 댓글분석 관련 파일들을 `youtubeComment/` 폴더로 정리하고, 참조 경로를 함께 수정한다.

## key changes
- `src/main/resources/static/docs/youtubeComment/` 디렉토리를 생성하고 아래 파일들을 이동했다.
  - `neg_pol_word.txt`
  - `obj_unknown_pol_word.txt`
  - `pos_pol_word.txt`
  - `SentiWord_Dict.txt`
  - `sentiment_lexicon.tsv`
  - `sentiment_custom.tsv`
- `SentimentLexiconLoader`의 classpath 경로를 새 위치로 변경했다.
- `PRD-youtubeComment` 문서의 참조 경로를 새 위치로 수정했다.

## touched files
- `src/main/java/springVibe/dev/users/youtubeComment/sentiment/SentimentLexiconLoader.java`
- `docs/PRD-youtubeComment.md`
- `src/main/resources/static/docs/youtubeComment/*` (moved)

## run/test commands
- `./mvnw -DskipTests compile`

## follow-ups
- 과거 `docs/aiwork/*`에 남아있는 “옛 경로” 표기는 기록이므로 그대로 두었다(필요 시 별도 마이그레이션 노트로 정리).
