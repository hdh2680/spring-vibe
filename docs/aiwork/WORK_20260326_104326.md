# 작업 노트 (AI Work Log)

## goal
- Elasticsearch가 내려가 있거나 사용하지 않는 환경에서도 애플리케이션이 부팅되도록 `postDocumentRepository` 생성 시점 실패를 제거한다.

## key changes
- `PostDocument`의 `@Document`에 `createIndex = false`를 설정하여, Spring Data Elasticsearch repository 생성 시점에 인덱스/매핑 자동 생성(및 ES 접속 시도)을 하지 않도록 했다.
- 인덱스 생성/매핑은 기존 로직(`DevSearchService.ensureIndex()`)에서 실제 기능 호출 시점에 처리하도록 유지했다.

## touched files
- `src/main/java/springVibe/dev/users/devSearch/search/PostDocument.java`

## run/test commands
- `./mvnw -DskipTests compile`

## follow-ups
- ES를 완전히 옵션으로 두려면 `spring.data.elasticsearch.repositories.enabled=false` + 프로파일/조건부 `@EnableElasticsearchRepositories` 구성으로 repository 자체를 비활성화하는 방법도 고려.
