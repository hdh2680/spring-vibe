# 작업 노트 (AI Work Log)

## goal
- Dev Search Elasticsearch 색인을 `title`, `body`, `tags` 필드만 사용하도록 축소한다.

## key changes
- `VelogPostDocument`에서 불필요 필드(username/urlSlug/canonicalUrl/shortDescription/releasedAt/updatedAt/likes/commentsCount)를 제거했다.
- 전체색인(reindex) 시에도 `title`, `body`, `tags`만 ES에 저장하도록 매핑 로직을 변경했다.
- 검색 쿼리 대상도 `title/body/tags`로 정리했다.

## touched files
- `src/main/java/springVibe/dev/users/devSearch/search/VelogPostDocument.java`
- `src/main/java/springVibe/dev/users/devSearch/search/DevSearchService.java`

## run/test commands
- `./mvnw -DskipTests compile`

## follow-ups
- 기존 인덱스에 남아있는 필드는 자동으로 제거되지 않으므로, 완전 반영을 원하면 인덱스를 삭제 후 재색인한다.
