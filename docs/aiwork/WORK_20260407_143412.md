# AI Work Log

- goal: Thymeleaf UI는 유지하고, 신규 메뉴를 React+TypeScript(Vite)로 `/app/**`에 병행 운영하는 방식의 초기 세팅/구조/운영 규칙을 문서화
- key changes: `frontend/` 기반 병행 운영 가이드 문서 추가(개발/배포 모드, `/app` 라우팅, SPA 딥링크 forward 개념, 권장 파일 구조/운영 룰)
- touched files:
  - `docs/etc/FRONTEND_REACT_TS_VITE.md`
- run/test commands:
  - `Get-ChildItem -Force docs`
  - `Get-Date -Format yyyyMMdd_HHmmss`
- follow-ups:
  - 실제 적용 시: Spring에 `/app/** -> /app/index.html` forward 라우트 추가, Vite 프록시 및 build base(`/app/`) 설정, CI에서 frontend build 후 정적 리소스 복사 흐름 확정

