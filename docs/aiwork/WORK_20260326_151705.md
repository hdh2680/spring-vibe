# 작업 노트 (AI Work Log)

## goal
- `local` 환경에서 JPA 쿼리(SQL)와 바인딩 파라미터를 로그로 확인할 수 있게 한다.

## key changes
- `application-local.yml`에 Hibernate SQL 출력/포맷 옵션을 추가했다.
  - `hibernate.format_sql=true`
  - `hibernate.highlight_sql=true`
- `application-local.yml`에 Hibernate 로깅 레벨을 추가했다.
  - `org.hibernate.SQL=DEBUG`
  - `org.hibernate.orm.jdbc.bind=TRACE`

## touched files
- `src/main/resources/application-local.yml`

## run/test commands
- `./mvnw -DskipTests compile`

## follow-ups
- 로그가 너무 많으면 `org.hibernate.orm.jdbc.bind`를 `DEBUG`로 낮추거나 필요할 때만 켜는 방식으로 운영한다.
