# 작업 노트 (AI)

## goal
- 유튜브 댓글 저장(JSONL) 버튼 클릭 시 화면 새로고침 없이 비동기식으로 처리한다.

## key changes
- `/users/youtubeComment/exportAsync` 엔드포인트를 추가해 JSON 응답으로 저장 결과(메시지/파일 경로)를 반환한다.
- 화면에서 저장 버튼 submit을 가로채 `fetch`로 `/exportAsync`를 호출하도록 처리했다. (검색 submit은 기존대로 동작)

## touched files
- `src/main/java/springVibe/dev/users/youtubeComment/controller/YoutubeCommentController.java`
- `src/main/resources/templates/html/users/youtubeComment/youtubeCommentSearch.html`

## run/test commands
- `.\mvnw.cmd -q test`

## follow-ups
- alert 대신 페이지 내 toast/상태 영역으로 UX 개선(파일 경로 copy 버튼 등).
- 저장 실패 시 재시도/중복 클릭 방지 UX 정교화.

