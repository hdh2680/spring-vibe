# 목표 (goal)
- 감정분석 탭의 시간별 변화 그래프에서 0%/100% 스파이크(요동)를 완화한다.

# 핵심 변경사항 (key changes)
- 시간별 변화 라인 차트 계산에 라플라스 스무딩(alpha=1)을 적용했다.
  - 저장된 분석 수치(JSON)는 변경하지 않고, **시각화(그래프)만** 스무딩된 값을 사용한다.
- 그래프 하단에 “스무딩 적용” 안내 문구를 추가했다.

# 수정/추가 파일 (touched files)
- `src/main/resources/templates/html/users/youtubeComment/youtubeCommentAnalysisView.html`
- `docs/aiwork/WORK_20260320_170211.md`

# 실행/테스트 명령 (run/test commands)
- (미실행) `mvnw -DskipTests compile`

# 후속 작업 (follow-ups)
- alpha 값을 UI에서 조절하거나, 표본 수가 작은 시간대는 점/툴팁으로만 표시하는 방식도 검토.
