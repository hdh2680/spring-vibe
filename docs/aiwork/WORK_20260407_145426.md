# AI Work Log

- goal: Thymeleaf 유지 + 신규 메뉴는 React+TS(Vite)로 `/app/**`에 병행 운영할 수 있게 "기본 세팅만" 추가
- key changes:
  - `frontend/` Vite(React+TS) 기본 프로젝트 추가: `/app` basename 라우팅, 최소 레이아웃/홈 화면
  - Vite dev proxy(`/api`, `/css`, `/images`) 기본 설정 추가
  - Spring `/app/**` SPA 딥링크 forward 컨트롤러 추가
  - Spring Security에서 `/app/**` permitAll 추가(초기 접근 가능하게)
  - `.gitignore`에 `frontend/node_modules`, `frontend/dist` 등 추가
  - 문서에 실행 예시(npm install/dev/build) 보완
- touched files:
  - `.gitignore`
  - `frontend/package.json`
  - `frontend/tsconfig.json`
  - `frontend/vite.config.ts`
  - `frontend/index.html`
  - `frontend/src/main.tsx`
  - `frontend/src/app/App.tsx`
  - `frontend/src/app/routes.tsx`
  - `frontend/src/app/ui/AppLayout.tsx`
  - `frontend/src/features/home/HomePage.tsx`
  - `frontend/src/api/http.ts`
  - `src/main/java/springVibe/dev/common/controller/AppSpaForwardController.java`
  - `src/main/java/springVibe/system/config/SecurityConfig.java`
  - `docs/etc/FRONTEND_REACT_TS_VITE.md`
- run/test commands:
  - `rg`
  - `Get-Content`
  - `Get-Date -Format yyyyMMdd_HHmmss`
- follow-ups:
  - 운영 배포 자동화를 위해: `frontend/dist`를 `src/main/resources/static/app/`로 복사하는 빌드 파이프라인(Maven/CI) 확정
  - `/api/**` 인증 정책이 필요하면 SecurityConfig에서 `permitAll` 재검토

