# 작업 노트 (2026-03-18 18:01:00 KST)

## goal
- PRD 기반 전처리 기능 보강: 원본 JSONL을 읽고, 원본 필드 + clean 결과를 추가한 새 JSONL 생성

## key changes
- 전처리 출력 JSONL 라인에 `textOriginal`, `textClean` 필드를 추가(기존 `text`는 유지)
- 전처리 파일 생성 경로를 원본 파일명 기반으로 생성: `{원본파일명}_clean_{yyyyMMdd_HHmmss}.jsonl`
- 상세 미리보기는 `textClean` 우선 표시(없으면 `text_clean` -> `text` fallback)

## touched files
- `src/main/java/springVibe/dev/users/youtubeComment/service/YoutubeCommentService.java`
- `docs/aiwork/WORK_20260318_180100.md`

## run/test commands
- `./mvnw -q -DskipTests compile` (Windows: `mvnw.cmd`)

## follow-ups
- 전처리 버전(`preprocess_version`)을 JSONL/DB에 기록할지 결정 필요

