# 작업 노트 (AI Work Log)

## goal
- Dev Search Elasticsearch 인덱스의 문서를 전체 삭제(deleteAll)할 수 있는 API를 제공한다.

## key changes
- `DevSearchService.clearAllDocuments()`를 추가하여 `VelogPostDocumentRepository.deleteAll()`로 전체 문서를 삭제하도록 했다(인덱스/매핑은 유지).
- `POST /api/dev-search/clear` 엔드포인트를 추가했다.

## touched files
- `src/main/java/springVibe/dev/users/devSearch/search/DevSearchService.java`
- `src/main/java/springVibe/dev/users/devSearch/web/DevSearchController.java`

## run/test commands
- `./mvnw -DskipTests compile`

## follow-ups
- 완전 초기화를 원하면(매핑까지) index delete + recreate API도 추가할 수 있다.
