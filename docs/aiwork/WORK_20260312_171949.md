# Recent Changes

## 2026-03-12 - Login + Sample Removal
- Removed all `Sample*` demo sources and MyBatis/JPA artifacts.
- Implemented DB-backed login using `users`/`roles`/`user_roles`.
- Added SHA-256 password hashing for login checks.
- Added a simple authenticated main page at `/`.

Touched areas:
- `system/config`: Spring Security + main route
- `system/security`: `UserDetailsService`, `PasswordEncoder`
- `dev/mapper` + `resources/mapper`: MyBatis mapper for users/roles

