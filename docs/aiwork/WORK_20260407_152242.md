# AI Work Log

- goal: `/app` 개발 서버에서 Spring CSS 공유(brand/layout_app)가 실제로 적용되도록 Vite base 설정 문제를 수정
- key changes:
  - Vite `base`를 dev/serve에서는 `/`, build에서는 `/app/`로 분기
    - dev에서 `/css/...` 링크가 `/app/css/...`로 변환되는 문제를 방지해 프록시(`/css -> :8080`)가 동작하도록 함
- touched files:
  - `frontend/vite.config.ts`
- run/test commands:
  - `Invoke-WebRequest http://localhost:5173/app/` (링크 경로 확인)
  - `Get-Date -Format yyyyMMdd_HHmmss`
- follow-ups:
  - Vite dev server 재시작 필요(`vite.config.ts` 변경 반영)

