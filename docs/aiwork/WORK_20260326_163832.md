# 작업 노트 (AI Work Log)

## goal
- DevSearch 상세 화면에서 `velog_post.body`가 Markdown일 때 Markdown 문법을 HTML로 변환해 보기 좋게 렌더링한다.

## key changes
- Markdown -> HTML 렌더링 서비스 추가:
  - `commonmark`로 Markdown 파싱/렌더링
  - raw HTML escape + `jsoup` sanitize로 XSS 방지
- 상세 화면에서 렌더링된 HTML을 `th:utext`로 출력하도록 변경
- Markdown 렌더링용 기본 스타일(CSS) 추가

## touched files
- `pom.xml`
- `src/main/java/springVibe/dev/users/devSearch/service/MarkdownRenderService.java`
- `src/main/java/springVibe/dev/users/devSearch/controller/DevSearchPagesController.java`
- `src/main/resources/templates/html/users/devSearch/view.html`
- `src/main/resources/static/css/layout_app.css`

## run/test commands
- `./mvnw -DskipTests compile`

## follow-ups
- Markdown 자동완성/코드 하이라이팅(prism 등)이 필요하면 프론트에서 추가 적용 가능.
