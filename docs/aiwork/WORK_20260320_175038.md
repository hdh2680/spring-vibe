# 목표 (goal)
- 네트워크 분석 탭에서 `Top Edges / Top Nodes`를 제거하고, 핵심 정보(요약 + 그래프)만 보여준다.

# 핵심 변경사항 (key changes)
- 네트워크 탭 하단의 `Top Edges / Top Nodes` UI 패널을 삭제했다.
- `renderWordNetwork()`에서 테이블 렌더링 로직과 관련 CSS를 제거했다(그래프/요약만 렌더링).

# 수정/추가 파일 (touched files)
- `src/main/resources/templates/html/users/youtubeComment/youtubeCommentAnalysisView.html`
- `docs/aiwork/WORK_20260320_175038.md`

# 실행/테스트 명령 (run/test commands)
- `mvnw -DskipTests compile`

# 후속 작업 (follow-ups)
- 필요하면 “상위 연결 노드 라벨 개수”만 UI 옵션으로 제공한다.
