# 작업 노트 (AI Work Log)

## goal
- 로컬에서 Elasticsearch와 Kibana를 함께 띄워 Dev Search 인덱스/쿼리를 쉽게 확인할 수 있게 한다.

## key changes
- `docker-compose.dev-search.yml`에 Kibana 서비스를 추가했다.
  - 포트: `5601:5601`
  - ES 의존: `depends_on` + `service_healthy`
- `PRD-devSearch`에 Kibana 접속 URL을 추가했다.

## touched files
- `docker-compose.dev-search.yml`
- `docs/PRD-devSearch.md`

## run/test commands
- `docker compose -f docker-compose.dev-search.yml config`

## follow-ups
- Kibana에서 `dev-search-posts` 인덱스 확인을 위해 Data View 생성(`dev-search-posts*`) 절차를 문서에 추가할 수 있다.
