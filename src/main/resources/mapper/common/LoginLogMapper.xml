<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="basemarkdown.dev.common.mapper.LoginLogMapper">

    <insert id="insert" parameterType="basemarkdown.dev.common.domain.LoginLog" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO login_logs (user_id, username, login_ip, user_agent, login_result, login_at)
        VALUES (#{userId}, #{username}, #{loginIp}, #{userAgent}, #{loginResult}, NOW())
    </insert>

</mapper>

